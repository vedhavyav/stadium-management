import { UserLocation } from './types';

/**
 * Constants used across the application
 */
export const FAN_START_LOCATION: UserLocation = { zone: "Section 104", x: 26, y: 35 };
export const SIMULATION_INTERVAL_MS = 5000; // 5 seconds
export const DEFAULT_VOLUNTEER_ID="VOL-001";
